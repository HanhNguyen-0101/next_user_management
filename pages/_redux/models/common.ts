export interface DrawerPayload {
  visible?: boolean | false,
  title?: string | '',
  FormComponent: null,
}

export interface ModalPayload {
  visible?: boolean | false,
  title?: string | '',
  FormComponent: null,
}